# T5 브리프 — 주소 품질 검사 + 지도용 좌표 산출

## 소유 파일
- `services/crawler/profiling/address.py` (신규)
- `services/crawler/profiling/geo.py` (신규)

## 작업
T3의 컬럼 스키마를 입력으로:

1. **주소 컬럼 탐지** — 도로명 / 지번 / 우편번호 패턴.
2. **주소 형식 유효성과 품질 점수** 산출.
3. **좌표 검증** — 위경도 컬럼이 이미 있으면 대한민국 경계 범위 검증,
   없으면 지오코딩 대상으로 표기.

## 결정적 제약 — 실제 지오코딩 호출 금지
**이 저장소는 외부 인증키를 보유하지 않는다** (CLAUDE.md 명시). 따라서
지오코딩은 **인터페이스와 오프라인 검증만** 구현하고 실제 API 호출은 하지 마라.
좌표가 확인되지 않은 항목은 반드시 `unverified`로 표기하라.

## 지도 뷰 범위
**GeoJSON 산출까지가 범위다. 프론트엔드는 만들지 마라.** 대시보드는 별도 모듈
(`apps/dashboard`)이고 이 작업 범위 밖이다.

## 공통 규약

- 착수 전 `docs/superpowers/plans/2026-08-01-crawler-depth-and-link-harvest.md` 정독.
- **소유 파일 밖 수정 절대 금지.** `main.py`, `requirements.txt`, `README.md`,
  `CLAUDE.md`, `managers/file_storage.py`는 조정자 소유다. 배선이 필요하면
  직접 고치지 말고 `worker_done`에 보고하라.
- `services/crawler/tests/` 아래 신규 테스트 파일 생성은 허용. 네트워크 없이
  도는 fixture 기반이어야 한다.
- 새 런타임 의존성은 `requirements.txt`를 고치지 말고 `worker_done`에
  "필요 패키지: X>=Y"로 보고하라.
- 검증 못 한 것은 `unverified`로 표기. 추정을 사실로 쓰지 마라.
- `AskUserQuestion` 사용 금지 (조정자가 볼 수 없어 세션이 영구 정지한다).
  `orca orchestration ask`를 쓰거나, 가정을 세우고 진행 후 명시하라.

## 선행 작업 (T3) — 이미 완료됨

`services/crawler/profiling/` 패키지가 이미 있다:
- `fetcher.py` — Range 샘플링 기반 스트리밍 수신, 호스트별 동시성/딜레이/
  robots.txt 준수. 전량 수신은 명시 옵션 뒤에 있다.
- `schema_infer.py` — CSV/TSV/JSON에서 컬럼명·타입·널 비율·샘플값 산출.
  cp949/euc-kr 포함 인코딩 감지 포함. 표준 라이브러리만 사용(추가 패키지 없음).

**착수 전 이 두 파일을 읽어 실제 시그니처와 반환 구조를 확인하라. 추측 금지.**
기존 테스트 24개가 통과하는 상태다. 깨뜨리지 마라.

## 완료 보고 (필수 — 실패해도 1회)

```
orca orchestration send --to term_d6be2f7a-abc5-423c-ade2-78731a0afe82 --from term_76665cfb-3135-4051-9e52-08f91ea329b1   --type worker_done --subject "<짧은 상태>"   --body "<3문장: 한 것 / 발견한 것 / 남은 것>"   --task-id task_e97805324c08 --dispatch-id ctx_c4148b5365f8   --files-modified "경로1,경로2"
```

보고 후 턴을 끝내고 대기하라. `check` 폴링 금지.

# T6 브리프 — 산개 기관 포털 하베스터 (Phase B)

## 소유 파일
- `services/crawler/portals/` 신규 패키지 전체

## 배경
LINK형은 각 기관이 data.go.kr에 통합하지 않고 **자기 포털에 산개시켜 둔** API들이다.
수백~수천 개 포털에 흩어져 있다. T1이 LINK 레코드에서 외부 엔드포인트 URL을
추출하도록 이미 구현했다 (`CrawlData.external_endpoint_urls`).

## 핵심 원칙 — 어댑터를 수백 개 쓰지 않는다
호스트를 클러스터링한 뒤, 호스트별로 **프로토콜을 감지**해 5종 어댑터로 커버한다:

| 순서 | 프로브 | 어댑터 |
| --- | --- | --- |
| 1 | `/api/3/action/package_list` | CKAN |
| 2 | `/v2/api-docs`, `/swagger.json`, `/openapi.json`, `/v3/api-docs` | OpenAPI 직수집 |
| 3 | `/robots.txt` → `sitemap.xml` | 사이트맵 |
| 4 | schema.org `Dataset` JSON-LD | DCAT/JSON-LD |
| 5 | 전부 실패 | generic(제목·표만) + `unverified` 표기 |

호스트 분포는 롱테일이다. 상위 수십 개 호스트가 대부분의 건수를 차지하므로
**호스트별 건수를 실측 집계**하고 상위부터 처리하는 구조로 만들어라.

## 필수 안전장치 (타협 금지)
- `robots.txt` 존중
- 호스트별 동시성 **1~2** 상한, 요청 간 딜레이, 타임아웃
- 감지 프로브는 호스트당 **최소 횟수**로 설계 (5종을 다 쏘지 말고 조기 종료)
- 실패 시 조용한 폴백 금지 — 무엇을 시도했고 왜 실패했는지 근거를 남겨라

## 테스트
`services/crawler/tests/`에 fixture 기반 테스트를 넣되,
**실제 기관 포털을 테스트에서 호출하지 마라.**

## 공통 규약

- 착수 전 `docs/superpowers/plans/2026-08-01-crawler-depth-and-link-harvest.md`를 정독하라.
- **소유 파일 밖은 절대 수정 금지.** 특히 `main.py`, `requirements.txt`,
  `README.md`, `CLAUDE.md`, `managers/file_storage.py`는 조정자 소유다.
  배선이 필요하면 직접 고치지 말고 `worker_done` 본문에 보고하라.
- `services/crawler/tests/` 아래 **신규 테스트 파일 생성은 허용**한다.
  네트워크 없이 도는 fixture 기반이어야 한다. 기존 `tests/conftest.py` 패턴을 따르라.
- `find_project_root()` 복제본을 새로 만들지 마라 (CLAUDE.md의 4곳 동기화 제약).
- 새 런타임 의존성은 `requirements.txt`를 고치지 말고 `worker_done`에
  "필요 패키지: X>=Y"로 보고하라.
- 검증 못 한 것은 `unverified`로 표기하라. 추정을 사실로 쓰지 마라.
- `AskUserQuestion`을 쓰지 마라 (조정자가 볼 수 없어 세션이 영구 정지한다).
  질문은 `orca orchestration ask`를 쓰거나, 합리적 가정을 세우고 진행한 뒤
  `worker_done`에 그 가정을 명시하라.

## 이미 완료된 선행 작업 (T1)

`openapi_crawler.py`와 `file_data_crawler.py`는 이미 갱신됐다. 새로 생긴 것:

- `_detect_api_type()`가 이제 `openapi_link` / `openapi_new` /
  `openapi_parse_failed` 셋으로 분류한다.
- `CrawlData.api_type_evidence` — 분류 근거 딕셔너리.
- `CrawlData.external_endpoint_urls` — LINK형 페이지에서 추출한 외부 기관
  엔드포인트 URL 목록.
- `file_data_crawler`의 JSON-LD 파싱이 Dataset 블록 전체를 보존한다.

착수 전 이 3개 파일을 읽어 실제 시그니처를 확인하라. 추측하지 마라.

## 완료 보고 (필수 — 실패해도 반드시 1회)

```
orca orchestration send --to term_d6be2f7a-abc5-423c-ade2-78731a0afe82 --from term_d1f1f2f0-62b0-411a-980a-5b5e0d7d3466   --type worker_done --subject "<짧은 상태>"   --body "<3문장: 무엇을 했는지 / 무엇을 발견했는지 / 무엇이 남았는지>"   --task-id task_3fc89e59d61e --dispatch-id ctx_6ce39817ef4f   --files-modified "경로1,경로2"
```

보고 후 턴을 끝내고 프롬프트에서 대기하라. `check`를 폴링하지 마라.

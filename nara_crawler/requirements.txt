# Runtime dependencies for the current Stage 1/2/3 pipeline.

# Stage 1 raw crawling
aiohttp>=3.9.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
requests>=2.31.0
tqdm>=4.66.0

# Data validation models used by the crawler
pydantic>=2.0.0

# Stage 5 index — ChromaDB vector store + Korean-capable embedding model
chromadb>=1.0.0
sentence-transformers>=3.0.0

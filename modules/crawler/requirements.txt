# Runtime dependencies for modules/crawler.
# Keep this file limited to packages imported by crawler code.

aiohttp>=3.9.0
beautifulsoup4>=4.13.0
lxml>=4.9.0
requests>=2.31.0
tqdm>=4.66.0
pydantic>=2.0.0

# backend/ (브라우저 제어 UI). 크롤 자체는 CLI 서브프로세스로 돈다.
fastapi>=0.110.0
uvicorn>=0.27.0
